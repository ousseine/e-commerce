<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="#!">Shop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                <li class="nav-item"><a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a></li>
                <li class="nav-item"><a class="nav-link" href="#!">A propos</a></li>
                <li class="nav-item"><a class="nav-link" href="#!">Contact</a></li>
            </ul>
            <div class="d-flex align-items-center">
                <a href="{{ path('app_cart') }}" class="btn btn-outline-dark" type="submit">
                    <i class="bi-cart-fill me-1"></i>
                    Panier
                    <span class="badge bg-dark text-white ms-1 rounded-pill">
                        {{ app.session.get('cart') ? app.session.get('cart').elements | length : 0  }}
                    </span>
                </a>

                {% if app.user %}
                    <div class="btn-group ms-4">
                        <span type="button" data-bs-toggle="dropdown" data-bs-display="static" aria-expanded="false">
                            <i class="bi bi-person-fill" style="font-size: 1.5rem; color: black;"></i>
                        </span>
                        <ul class="dropdown-menu dropdown-menu-sm-start">
                            <li>
                                <a href="{{ path('app_profile') }}" class="dropdown-item d-flex align-items-center"><i class="bi bi-person-fill"></i>&nbsp; Mon compte</a>
                            </li>
                            <li>
                                <a href="{{ path('app_logout') }}" class="dropdown-item"><i class="bi bi-box-arrow-left"></i>&nbsp; Logout</a>
                            </li>
                        </ul>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}" class="ms-4">
                        <i class="bi bi-box-arrow-right" style="font-size: 1.5rem; color: black;"></i>
                    </a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>